# Equilibrium-Simulator

## Objective
* Simulate mechanical equilibrium of an object on a plane,
* Exactly draw the force vectors applied to the object,
* Make every information accessible to the user, so he can simulate different conditions by changing every values, from the environment to the object etc...

## Future
* Make it a real time simulation. The user should be able to input data and the graph should update immediately with the new data.